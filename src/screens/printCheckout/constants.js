export const MIN_RETAIL_CENTS = 50

/** Must match quote and checkout; backend default is 1 if omitted. */
export const PRINT_CHECKOUT_QUANTITY = 1

export const STEPS = ['Preview your book', 'Shipping & print options', 'Payment']
